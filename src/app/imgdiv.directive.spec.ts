import { ImgdivDirective } from './imgdiv.directive';

describe('ImgdivDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgdivDirective();
    expect(directive).toBeTruthy();
  });
});
