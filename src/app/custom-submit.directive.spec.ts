import { CustomSubmitDirective } from './custom-submit.directive';

describe('CustomSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
