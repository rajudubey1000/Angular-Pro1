import { HeadingHighlightDirective } from './heading-highlight.directive';

describe('HeadingHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
